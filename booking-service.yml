# =============================================================================
# BOOKING SERVICE CONFIG
# =============================================================================
# Fetched ONLY by booking-service from Config Server.
# Common settings (eureka, jpa defaults) come from application.yml automatically.
# =============================================================================

server:
  port: 5003

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/booking_db
    username: root
    password: root
